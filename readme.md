# Ze Project

## 🚀 Запуск:

```bash
python3 process_input_bin.py
```

## 🔧 Алгоритм работы

### 1. Инициализация
- Из файла input/input.bin по очереди копируются `Chunk` размером с `ChunkSize`
- Инициализируются два процессора: `beginning` (чтение `Chunk` с начала) и `inverse` (чтение `Chunk` с конца) для обработки по `Crumb`.

### 2. Работа процессоров beginning и inverse

**Общая логика для обоих процессоров:**

#### **Алгоритм обработки Crumb с Байесовским предсказанием**

Алгоритм обработки каждого `Crumb` состоит из нескольких ключевых этапов, включая продвинутое байесовское предсказание:

1. **Байесовское предсказание** - попытка предсказать следующий Crumb на основе Beta-распределения:
   - Используется априорное распределение Beta(α, β) где α=1.0, β=1.0
   - Рассчитывается апостериорная вероятность для каждого кандидата
   - Вычисляется уверенность предсказания на основе дисперсии распределения
   - Если уверенность превышает порог (0.7), используется предсказание
   - Статистика успешных предсказаний обновляется в реальном времени

2. **Стандартная обработка** (если байесовское предсказание не сработало):
   - **Отсортировать счетчики** (по убыванию `Value`)
   - **Проверить зону актуализации** (если в зоне актуализации совпадение: `Matches` + 1, значению счетчика + `PredictIncrement`)
   - **Проверить остальные** (если совпадение вне зоны актуализации: `Matches` + 1, значению счетчика + `Increment`)
   - **Создать новый** (если нет совпадений → создать счетчик с `ID = значение Crumb`)

3. **Управление памятью**:
   - **Проверка пределов** - при достижении максимального значения счетчика (0xFFFFFFFF) все счетчики делятся на 2
   - **Сброс статистики** - при достижении максимума статистических значений вся статистика делится на 2

4. **Сохранение состояний** (запись изменений в `*.bin`, `*_matches.bin` и `*_bayes.bin` файлы)

---

## 🔄 Различие между Beginning и Inverse процессорами

### **Beginning Processor**
- **Направление обработки**: Чтение данных с начала файла
- **Порядок Crumb'ов**: Обрабатывает Chunk'и в прямом порядке
- **Пример**: Для `Chunk = [0x01, 0x02, 0x03, 0x04, 0x05]` обрабатывает:
  - `[0x01, 0x02]` → `[0x03, 0x04]` → `[0x05, 0x00]` (дополнение нулем)
- **Оптимален для**: Данных с последовательными паттернами, текстовых данных

### **Inverse Processor** 
- **Направление обработки**: Чтение данных с конца файла  
- **Порядок Crumb'ов**: Обрабатывает Chunk'и в обратном порядке
- **Пример**: Для `Chunk = [0x01, 0x02, 0x03, 0x04, 0x05]` обрабатывает:
  - `[0x05, 0x04]` → `[0x03, 0x02]` → `[0x01, 0x00]` (дополнение нулем)
- **Оптимален для**: Данных с концевыми паттернами, сжатых данных, бинарных форматов

**Преимущества двух подходов:**
- **Beginning**: Лучше обнаруживает паттерны в начале последовательностей
- **Inverse**: Эффективен для данных с важной концевой информацией
- **Комбинированный анализ**: Позволяет выявить различные типы паттернов в данных

---

## 🏛️ Иерархические Байесовские Модели

### **Обзор архитектуры**

Система использует многоуровневые Байесовские модели для улучшения предсказательной способности:

```
Уровень 3: Контекстные модели
    ↓
Уровень 2: Групповые модели  
    ↓
Уровень 1: Индивидуальные Crumb'ы
    ↓
Уровень 0: Наблюдаемые данные
```

### **Детальное описание уровней**

#### **Уровень 1: Индивидуальные Crumb'ы (Базовый Байес)**
- **Модель**: Beta-Binomial распределение
- **Априор**: Beta(α=1.0, β=1.0) - равномерное распределение
- **Обучение**: Обновление параметров на основе успехов/неудач предсказаний
- **Вероятность**: `P(success) = (α + успехи) / (α + β + общее_количество)`

#### **Уровень 2: Групповые модели**
- **Группировка**: Crumb'ы автоматически распределяются по группам на основе их значений
- **Размер группы**: Настраивается через `GROUP_SIZE` (по умолчанию 8)
- **Гиперпараметры**: Каждая группа имеет свои α, β параметры
- **Априор гиперпараметров**: `α_hyper = 2.0`, `β_hyper = 2.0`
- **Преимущество**: Совместное обучение схожих Crumb'ов

**Пример группировки:**
```python
# Crumb значение 0x1234 попадает в группу:
group = 0x1234 % GROUP_SIZE  # GROUP_SIZE = 8 → group = 4
```

#### **Уровень 3: Контекстные модели**
- **Контекст**: Учитывает последовательности предыдущих Crumb'ов
- **Глубина контекста**: Настраивается через `CONTEXT_DEPTH` (по умолчанию 3)
- **Пример контекста**: `[0xABCD, 0xEF01, 0x2345] → предсказание 0x6789`
- **Статистика**: Для каждой уникальной последовательности хранится статистика успехов

### **Процесс иерархического предсказания**

1. **Получение кандидатов**: Топ-N наиболее частых Crumb'ов
2. **Групповая оценка**: Для каждого кандидата вычисляется групповая вероятность
3. **Контекстная коррекция**: Вероятность корректируется на основе текущего контекста
4. **Взвешенное объединение**: Итоговая вероятность = f(групповая, контекстная)
5. **Выбор лучшего**: Кандидат с максимальной вероятностью выше порога

**Математическая формула:**
```
P_final = (P_group * W_group + P_context * W_context) / (W_group + W_context)
где:
  P_group = α_group / (α_group + β_group)
  P_context = успехи_контекста / общее_контекста
  W_group = α_group + β_group  # Вес группы
  W_context = min(10, W_group / 2)  # Ограниченный вес контекста
```

### **Пример работы иерархической модели**

```
Текущий контекст: [0x1234, 0x5678, 0x9ABC]
Кандидаты: [0xDEF0, 0x1111, 0x2222]

Для кандидата 0xDEF0:
  - Группа: 0xDEF0 % 8 = 0
  - Групповые параметры: α=15, β=5 → P_group = 15/20 = 0.75
  - Контекст [0x1234,0x5678,0x9ABC]: успехи=8, всего=10 → P_context = 0.8
  - Вес группы: 20, вес контекста: min(10, 10) = 10
  - Итоговая вероятность: (0.75*20 + 0.8*10)/(20+10) = 0.766

Если вероятность > порога (0.7) → используется предсказание
```

### **Преимущества иерархического подхода**

1. **🎯 Улучшенная точность**: Учет групповых и контекстных паттернов
2. **📈 Быстрая адаптация**: Групповое обучение ускоряет сходимость
3. **🛡️ Устойчивость к шуму**: Иерархическое сглаживание уменьшает переобучение
4. **🔍 Обнаружение паттернов**: Выявление сложных зависимостей в данных
5. **🎪 Масштабируемость**: Эффективная работа с большими наборами данных

---

#### **Проверка на примере**
- **Входные данные**: `Chunk = [0x01, 0x02, 0x03, 0x04, 0x05]` (`CrumbSize = 2`)
- **Beginning**:
  - Обработает: `[0x01,0x02] → [0x03,0x04]`
  - Остаток: `[0x05]` (дополняется нулем: `[0x05,0x00]`)
- **Inverse**:
  - Обработает: `[0x05,0x04] → [0x03,0x02]`
  - Остаток: `[0x01]` (дополняется нулем: `[0x01,0x00]`)

---

## 📁 Структура проекта

```
Ze/
├── input/                 # Входные данные
│   └── input.bin          # Основной входной файл
├── output/                # Выходные данные
│   ├── begin.bin          # Счетчики beginning процессора
│   └── inverse.bin        # Счетчики inverse процессора
├── stats/                 # Статистика
│   ├── begin_matches.bin  # Статистика beginning процессора
│   ├── inverse_matches.bin # Статистика inverse процессора
│   ├── begin_bayes.bin    # Базовая байесовская статистика
│   └── inverse_bayes.bin  # Базовая байесовская статистика
├── hierarchical/          # Иерархические модели
│   ├── begin_groups.bin   # Групповые модели beginning
│   ├── inverse_groups.bin # Групповые модели inverse  
│   ├── begin_context.bin  # Контекстные модели beginning
│   └── inverse_context.bin # Контекстные модели inverse
├── config.py              # Конфигурационные параметры
├── process_input_bin.py   # Основной процессор
├── bayesian_predictor.py  # Базовый байесовский предсказатель
├── hierarchical_bayesian.py # Иерархические модели
├── audio_stream.py        # Аудио модуль
├── create_test_data.py    # Генератор тестовых данных
└── README.md              # Документация
```

## 💾 Файлы данных

### 1. **Основные файлы (`begin.bin` и `inverse.bin`)**:
   - **Расположение**: Папка `output/`
   - **Назначение**: Хранение счетчиков встречаемости Crumb'ов
   - **Формат**: `ключ (4 байта) + значение (4 байта)` для каждого элемента
   - **Ключ**: числовое представление Crumb (2-байтовой последовательности)
   - **Значение**: счетчик встречаемости этого Crumb
   - **Формат записи**: `struct.pack(">II", key, value)` (big-endian, два беззнаковых int)

## 📊 Файлы статистики

### 2. **Файл статистики (`begin_matches.bin` и `inverse_matches.bin`)**:
   - **Расположение**: Папка `stats/`
   - **Назначение**: Детальный анализ качества предсказаний
   - **Содержит 5 записей**:
     1. `0: total_matches_actualization` - совпадения в зоне актуализации
     2. `1: total_matches_others` - совпадения в остальных счетчиках
     3. `2: total_first_prediction_hits` - попадания с первого предсказания
     4. `3: total_crumbs` - общее количество обработанных Crumb'ов
     5. `4: total_bayesian_hits` - успешные байесовские предсказания
   - **Формат записи**: `struct.pack(">II", key, value)` (big-endian, два беззнаковых int)

## 🧠 Байесовские файлы

### 3. **Файлы базовой статистики (`begin_bayes.bin` и `inverse_bayes.bin`)**:
   - **Расположение**: Папка `stats/`
   - **Назначение**: Статистика базового байесовского предсказателя
   - **Содержит**:
     - **Заголовок** (16 байт): `total_predictions, successful_predictions, confident_predictions, hierarchical_used`
     - **Данные по Crumb'ам**: для каждого Crumb'а хранится `ключ, успехи, общее_количество`
   - **Формат записи**: 
     - Заголовок: `struct.pack(">IIII", total_predictions, successful_predictions, confident_predictions, hierarchical_used)`
     - Данные: `struct.pack(">III", crumb, successes, total_attempts)`

## 🏛️ Иерархические файлы

### 4. **Файлы групповых моделей (`*_groups.bin`)**:
   - **Расположение**: Папка `hierarchical/`
   - **Назначение**: Хранение параметров групповых Байесовских моделей
   - **Содержит**:
     - **Количество групп** (4 байта)
     - **Данные групп**: для каждой группы `group_id, alpha, beta`
   - **Формат записи**: 
     - Заголовок: `struct.pack(">I", group_count)`
     - Данные: `struct.pack(">Iff", group_id, alpha, beta)`

### 5. **Файлы контекстных моделей (`*_context.bin`)**:
   - **Расположение**: Папка `hierarchical/`
   - **Назначение**: Хранение контекстных последовательностей и их статистики
   - **Содержит**:
     - **Количество контекстов** (4 байта)
     - **Данные контекстов**: для каждого контекста `длина, crumb..., успехи, всего`
   - **Формат записи**: 
     - Заголовок: `struct.pack(">I", context_count)`
     - Данные: `struct.pack(">I", context_length)` + `struct.pack(">I", crumb)` × N + `struct.pack(">II", successes, total)`

## 🎯 Статистика работы

После обработки выводятся детальные метрики:

### **Базовый байесовский предсказатель:**
- **Всего предсказаний** - общее количество попыток предсказания
- **Уверенных предсказаний** - предсказания с уверенностью выше порога
- **Успешных предсказаний** - корректные предсказания
- **Использовано иерархических** - количество использований иерархических моделей
- **Точность** - процент успешных предсказаний от уверенных
- **Отслеживаемых Crumb'ов** - количество Crumb'ов с накопленной статистикой

### **Иерархическая Байесовская модель:**
- **Групп** - количество активных групповых моделей
- **Контекстных моделей** - количество уникальных контекстных последовательностей
- **Иерархических предсказаний** - предсказания с использованием иерархических моделей
- **Успешных иерархических предсказаний** - корректные иерархические предсказания
- **Точность иерархических моделей** - эффективность иерархического подхода

## ⚙️ Конфигурация

Все параметры настраиваются в `config.py`:

### Основные параметры:
- `CHUNK_SIZE = 4096` - размер chunk'а в байтах
- `CRUMB_SIZE = 2` - размер crumb'а в байтах (2 байта = 16 бит)
- `MAX_COUNTER_VALUE = 0xFFFFFFFF` - максимальное значение для счетчиков
- `MAX_STATS_VALUE = 0xFFFFFFFF` - максимальное значение для статистики

### Параметры алгоритма:
- `PREDICT_INCREMENT = 5` - инкремент для зоны актуализации
- `INCREMENT = 1` - инкремент для остальных зон
- `ACTUALIZATION_RATIO = 0.2` - доля счетчиков в зоне актуализации (20%)

### Базовые байесовские параметры:
- `BAYES_ALPHA = 1.0` - Alpha параметр для априорного распределения
- `BAYES_BETA = 1.0` - Beta параметр для априорного распределения
- `CONFIDENCE_THRESHOLD = 0.7` - порог уверенности для байесовского предсказания
- `MIN_OBSERVATIONS = 10` - минимальное количество наблюдений для байесовского предсказания

### Иерархические байесовские параметры:
- `HIERARCHICAL_ENABLED = True` - Включить иерархические модели
- `GROUP_SIZE = 8` - Размер группы для иерархического анализа
- `HIERARCHICAL_ALPHA_PRIOR = 2.0` - Априорное значение для alpha гиперпараметра
- `HIERARCHICAL_BETA_PRIOR = 2.0` - Априорное значение для beta гиперпараметра
- `CONTEXT_DEPTH = 3` - Глубина контекста для иерархического анализа

### Пути к файлам:
- `INPUT_FILE = "input/input.bin"`
- `OUTPUT_DIR = "output"`
- `STATS_DIR = "stats"`
- `HIERARCHICAL_DIR = "hierarchical"`

## 🎵 Аудио модуль

Проект включает дополнительный аудио модуль (`audio_stream.py`), который:
- Записывает аудио в реальном времени
- Анализирует аудио данные с использованием созданных счетчиков
- Визуализирует уровень звука
- Сохраняет аудио в файл для последующего анализа

Запускается автоматически после завершения обработки данных.

---

## 🚀 Быстрый старт

```bash
# 1. Клонировать репозиторий
git clone git@github.com:djabbat/Ze.git
cd Ze

# 2. Создать тестовые данные
python3 create_test_data.py

# 3. Запустить обработку
python3 process_input_bin.py

# 4. (Опционально) Работа с аудио модулем
python3 audio_stream.py
```

---

## 📈 Пример вывода

```
ОБРАБОТКА ЗАВЕРШЕНА!
Обработано chunk'ов: 256
Beginning процессор - Crumb'ов: 524288, Счетчиков: 1500
Inverse процессор - Crumb'ов: 524288, Счетчиков: 1450

📊 Байесовский предсказатель begin:
   Всего предсказаний: 524288
   Уверенных предсказаний: 12500
   Успешных предсказаний: 9800
   Использовано иерархических: 8500
   Точность: 78.40%
   Отслеживаемых Crumb'ов: 1500

🏛️  Иерархическая Байесовская модель begin:
   Групп: 8
   Контекстных моделей: 1200
   Иерархических предсказаний: 8500
   Успешных предсказаний: 7200
   Точность: 84.70%
```

---

**Beginning и Inverse процессоры работают параллельно**, анализируя данные с разных направлений, а **иерархические Байесовские модели** обеспечивают максимальную точность предсказаний через многоуровневое обучение! 🔄🏛️🎯
```